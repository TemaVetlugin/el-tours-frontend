import { makeObservable, observable } from "mobx";

import { Model } from "./Model";

export interface {{pascalCase name}}ModelInterface {
{{{modelTypes properties}}}
}

export class {{pascalCase name}}Model extends Model<I{{pascalCase name}}Model> implements {{pascalCase name}}ModelInterface {
    fillable: Array<keyof I{{pascalCase name}}Model> = [
{{{modelFillable properties}}}
    ];

{{{modelProperties properties}}}

    constructor(payload?: {{pascalCase name}}ModelInterface) {
        super();

        makeObservable(this, {
{{{modelObservables properties}}}
        });

        this.update(payload);
    }
}
