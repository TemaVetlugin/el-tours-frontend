import React from "react";
import { observer } from "mobx-react";

import './index.scss';

type PropsType = {
}

export const C{{pascalCase name}} = observer(({  }: PropsType) => {
    return (
        <div className="c-{{kebabCase name}}">
        </div>
    )
});
